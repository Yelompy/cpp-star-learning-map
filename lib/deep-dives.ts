export interface DeepDive {
  mechanism: string;
  details: string[];
  boundary: string;
  verify: string;
}

export const deepDives: Record<string, DeepDive> = {
  "dev-environment": {
    mechanism: "一份源代码会先经过预处理，随后每个翻译单元独立编译成目标文件，最后由链接器解析跨文件符号并生成程序。编辑器只负责帮助书写，真正决定语言行为的是编译器、标准库和构建参数。",
    details: ["#include 本质上在预处理阶段引入声明；同一声明被重复定义会触发 ODR 问题。", "编译错误发生在语法或类型检查阶段；undefined reference 等链接错误说明声明可见但定义没有进入最终链接。", "Debug 构建保留调试信息并减少优化；Release 构建会改变指令布局，未定义行为有时只在优化后暴露。"],
    boundary: "GCC、Clang、MSVC 对标准支持和诊断文字不同；工程必须固定语言标准、目标架构和依赖版本，不能只说“在我的电脑上能运行”。",
    verify: "分别查看预处理输出、目标文件符号和最终可执行文件，并故意制造一处编译错误与一处链接错误。"
  },
  "program-structure": {
    mechanism: "一个 .cpp 与它包含的头文件组成翻译单元。声明告诉编译器名称和类型，定义真正分配对象或提供函数实现；作用域控制名称可见性，生命周期控制对象何时存在。",
    details: ["main 必须返回 int；正常到达末尾等价于 return 0。", "命名空间防止不同库使用相同名称时冲突，限定名 std::cout 明确了名称来源。", "头文件通常放声明，并使用 #pragma once 或 include guard 防止同一翻译单元重复包含。"],
    boundary: "头文件中的非 inline 普通函数定义可能在多个翻译单元产生重复定义；模板和 inline 实体遵循不同的 ODR 规则。",
    verify: "把一个程序拆成 main.cpp、math.hpp、math.cpp，分别观察遗漏声明、遗漏定义和重复定义时的报错阶段。"
  },
  "variables-types": {
    mechanism: "对象的类型同时决定内存解释方式、可表示范围、对齐要求和允许的运算；类型大小应由 sizeof、CHAR_BIT 与 numeric_limits 查询，不能把某个平台的常见结果当成语言保证。",
    details: ["整数保存离散值；无符号整数按模 2^N 运算，主流有符号整数使用补码。", "常见 IEEE 754 浮点数保存符号、指数和有效数，许多十进制小数无法被二进制有限精确表示。", "浮点转整数按标准丢弃小数并向 0 截断；若截断后的值超出目标范围，行为未定义。"],
    boundary: "64 位 Windows 常见 LLP64，64 位 Linux/macOS 常见 LP64；二者 int 通常都是 4 字节，但 long 大小不同。网络协议和磁盘格式应使用明确宽度与字节序。",
    verify: "打印各基础类型的 sizeof、alignof、numeric_limits 最小值与最大值，再验证 3.9、-3.9、0.1+0.2 的结果。"
  },
  "io": {
    mechanism: "iostream 把数据源抽象成流，并用状态位记录 good、eof、fail、bad。格式化提取运算符先跳过空白再按类型解析，而 getline 以分隔符为边界读取原始文本。",
    details: [">> 读取失败时目标值不应继续被信任，流会进入 fail 状态，后续读取也会失败。", "混用 >> 与 getline 时，前者通常把行末换行留在缓冲区，需要 ignore 或统一按行读取再解析。", "endl 会换行并强制刷新缓冲区；普通输出优先使用 '\\n'，避免不必要的系统调用。"],
    boundary: "终端编码、区域设置和 Windows/Unix 换行差异会影响文本处理；二进制文件还需处理端序、结构布局和版本兼容。",
    verify: "依次输入合法数字、字母、空行和超大数，打印流状态并完成 clear + ignore 的恢复流程。"
  },
  "operators": {
    mechanism: "表达式先根据类型规则进行整型提升和常用算术转换，再执行运算。结果类型往往在运行前就已确定，所以 int / int 仍得到 int，即使随后赋给 double。",
    details: ["5 / 2 先进行整数除法得到 2；写成 5.0 / 2 或显式转换才能得到 2.5。", "&& 与 || 从左到右短路求值，可用于先验证指针再解引用。", "有符号溢出是未定义行为；无符号溢出按模 2^N 回绕，但也不代表业务上合理。"],
    boundary: "不要在一个表达式里依赖复杂副作用顺序；可读的中间变量通常比记忆完整优先级表更可靠。",
    verify: "使用 decltype 输出不同混合运算的结果类型，并对 int/int、int/double、有符号/无符号比较做实验。"
  },
  "control-flow": {
    mechanism: "分支选择控制流图中的路径，循环则形成回边。正确循环需要初始化、持续条件、状态推进与可证明的终止条件。",
    details: ["for 的三个表达式分别适合初始化、继续条件和迭代步骤；范围 for 隐藏迭代器细节。", "break 结束最近一层循环，continue 跳到下一轮；嵌套循环需要明确作用层级。", "switch 默认不会自动停止，会继续落入后续 case；有意贯穿应标注 [[fallthrough]]。"],
    boundary: "边界错误通常来自区间模型不一致。标准库普遍使用左闭右开区间 [first,last)，手写循环最好保持同一约定。",
    verify: "为长度为 0、1、最大边界和正常长度分别手算循环次数，再用断言记录实际迭代次数。"
  },
  "functions": {
    mechanism: "函数调用会建立参数与局部对象的执行上下文。值参数创建新对象，引用参数绑定现有对象；返回值通常借助复制消除或移动直接构造在调用方。",
    details: ["小型标量按值传递清楚且高效，大型只读对象通常使用 const T&。", "重载解析综合名称、参数数量、转换等级和模板规则，不能只按返回类型重载。", "默认参数在调用点展开，应放在稳定声明处；递归函数还要考虑调用栈深度。"],
    boundary: "返回引用前必须证明被引用对象比调用结果活得久；返回局部变量引用会立即悬空。",
    verify: "为自定义类型记录复制/移动构造次数，比较值传递、const 引用与返回值优化。"
  },
  "arrays-strings": {
    mechanism: "数组是连续排列的同类型对象；std::string 在字符序列外维护长度与容量。下标运算不自动检查边界，at 则会检查并抛出异常。",
    details: ["原生数组传给多数函数会退化为首元素指针，长度信息丢失；std::array 保留长度。", "vector/string 的 size 是当前元素数，capacity 是无需重新分配即可容纳的元素数。", "UTF-8 中文字符通常占多个 char，因此 string::size 返回字节数而非人类字符数。"],
    boundary: "扩容会移动底层缓冲区，使旧指针、引用和迭代器失效；C 字符串还必须存在终止的零字符。",
    verify: "打印 vector 每次 push_back 后的 data、size、capacity，观察何时发生地址变化。"
  },
  "pointers-references": {
    mechanism: "指针对象保存地址，可为空并可重新赋值；引用是已存在对象的别名。解引用只在地址指向生命周期内的有效对象时有意义。",
    details: ["&value 取得地址，*pointer 访问所指对象；指针算术只在同一数组及尾后位置范围内有定义。", "const int* 表示不能通过指针修改对象，int* const 表示指针本身不能改指向。", "裸指针适合表达非拥有观察关系，所有权应由对象、容器或智能指针承担。"],
    boundary: "空指针、悬空指针、越界指针和错误对齐都会导致未定义行为；地址非零并不能证明对象仍有效。",
    verify: "画出对象、指针、引用的生命周期图，并用 AddressSanitizer 检测一次释放后使用。"
  },
  "struct-enum": {
    mechanism: "结构体把多个子对象组合为一个对象，编译器会为每个字段满足对齐要求并可能插入填充；enum class 创建具有独立作用域的有限值集合。",
    details: ["字段顺序会影响 padding 和 sizeof，按对齐从大到小排列有时能减少空间。", "聚合初始化按声明顺序填充字段，新增字段可能影响旧初始化代码。", "enum class 不会隐式转换为 int；需要底层数值时应显式转换。"],
    boundary: "不能直接把内存中的 struct 当作稳定网络或文件格式，因为 padding、端序、ABI 和编译器规则可能不同。",
    verify: "打印 offsetof、alignof 与 sizeof，调整字段顺序并比较布局变化。"
  },
  "memory-model": {
    mechanism: "对象生命周期从存储获得并完成初始化后开始，在析构或存储被复用时结束。自动、静态、线程和动态存储期决定存储持续时间，不完全等同于日常所说的栈和堆。",
    details: ["自动局部对象离开作用域时按构造逆序析构。", "new 同时分配存储并构造对象，delete 析构并释放；两者类型和形式必须匹配。", "内存泄漏不是唯一风险，重复释放、越界、未初始化读取和悬空访问更可能直接破坏程序。"],
    boundary: "placement new、union、对象重定位涉及更精确的生命周期规则；普通业务代码优先交给容器与 RAII。",
    verify: "使用构造/析构日志画出嵌套作用域顺序，再用 ASan/UBSan 检查故意制造的错误。"
  },
  "debugging": {
    mechanism: "调试的核心是把模糊现象转化为可复现输入，再通过观测证据逐步排除假设。断点、调用栈、监视表达式和内存检查各回答不同问题。",
    details: ["先保留最小复现和原始日志，再修改代码，避免证据被覆盖。", "调用栈解释程序如何到达当前点；条件断点可在特定数据出现时暂停。", "优化构建中变量可能被合并或消除，源码单步不一定与机器指令一一对应。"],
    boundary: "调试器看到的是某一次执行，不能证明不存在竞态；并发缺陷还需 ThreadSanitizer、事件追踪或确定性重放。",
    verify: "给一个确定输入，写下预期状态，逐语句比较实际状态，并在修复后增加自动回归测试。"
  },
  "classes": {
    mechanism: "类把表示和操作绑定在一起，通过访问控制维护不变量。对象布局包含非静态数据成员，成员函数通常不逐对象占用空间。",
    details: ["public 是稳定契约，private 是可自由重构的实现细节。", "const 成员函数不能通过 this 修改普通成员；mutable 仅应服务于缓存或同步等逻辑常量。", "成员初始化顺序由声明顺序决定，而不是初始化列表书写顺序。"],
    boundary: "getter/setter 并不自动等于封装；若调用者能随意制造非法组合，类仍未保护业务不变量。",
    verify: "列出类的所有有效状态和转换，尝试从公开接口构造非法状态，若能成功就收紧接口。"
  },
  "constructors": {
    mechanism: "构造过程先初始化基类，再按声明顺序初始化成员，最后执行构造函数体；析构顺序完全相反。初始化列表不是赋值语法，而是选择每个子对象如何构造。",
    details: ["引用、const 成员和无默认构造的成员必须在初始化列表中初始化。", "explicit 阻止单参数构造函数参与意外隐式转换。", "若类直接管理资源，复制、移动与析构必须形成一致策略；优先通过成员类型遵循零法则。"],
    boundary: "构造期间抛异常时，已构造的基类和成员会自动析构，但对象自身析构函数不会执行。",
    verify: "为基类和多个成员添加日志，改变初始化列表顺序并观察真实构造/析构顺序。"
  },
  "inheritance": {
    mechanism: "公有继承建立可替换的 is-a 关系，派生对象包含基类子对象。虚函数通过动态类型选择最终重写实现。",
    details: ["override 让编译器检查签名是否真的覆盖基类虚函数。", "用于多态删除的基类必须有虚析构，否则经基类指针 delete 派生对象是未定义行为。", "protected 会扩大耦合面；组合通常能更清楚地表达 has-a 关系。"],
    boundary: "私有继承和多重继承有特定用途，但会带来布局、名称查找和菱形继承复杂度。",
    verify: "用基类引用替换不同派生对象，检查前置条件、后置条件和异常保证是否仍成立。"
  },
  "polymorphism": {
    mechanism: "运行时多态依赖对象的动态类型，虚调用通常经虚表间接分派；编译期多态则由模板或重载在编译期生成具体调用。",
    details: ["按值复制派生对象到基类会发生切片，派生部分被丢弃。", "虚函数调用有一次间接寻址成本，也可能阻碍内联，但通常应先测量。", "variant + visit 可表达封闭类型集合，虚接口适合开放扩展集合。"],
    boundary: "构造和析构期间的动态类型受当前构造层次限制，不会分派到尚未构造或已析构的派生部分。",
    verify: "分别用值、引用和 unique_ptr<Base> 传递派生对象，观察类型、析构和调用差异。"
  },
  "operator-overload": {
    mechanism: "重载运算符其实是特殊命名的函数，编译器仍按重载解析选择它。它不能改变操作数数量、优先级或内置求值规则。",
    details: ["对称二元运算常写为非成员，使左右操作数享有相同转换机会。", "前置 ++ 通常返回 T&，后置 ++ 通过无意义 int 参数区分并返回旧值。", "C++20 的 <=> 可生成一致的排序关系，但仍需保证领域语义。"],
    boundary: "重载逗号、&&、|| 会失去内置短路或顺序直觉，通常不应这样设计。",
    verify: "为值类型写代数性质测试：a==a、a+b==b+a，以及相等对象哈希一致。"
  },
  "templates-basics": {
    mechanism: "模板本身是生成代码的规则，只有实例化时才对具体类型进行完整检查。定义通常必须在实例化点可见，因此常放在头文件。",
    details: ["类型推导不会执行任意隐式推理，引用折叠与 cv 限定会影响结果。", "依赖模板参数的名称需要 typename 或 template 帮助解析。", "每个不同模板实参组合都可能生成代码，带来编译时间与二进制体积成本。"],
    boundary: "显式实例化可以把常用组合集中到一个翻译单元；concepts 能在接口处表达要求并改善诊断。",
    verify: "让模板分别接收满足与不满足要求的类型，定位报错是在调用点还是模板体内发生。"
  },
  "containers": {
    mechanism: "容器把元素存储策略、容量管理和迭代接口封装起来。复杂度保证描述随元素数量增长的操作成本，但实际速度还受连续内存和缓存局部性影响。",
    details: ["vector 连续存储，随机访问 O(1)，尾部插入摊还 O(1)。", "map 通常是平衡树并保持键有序；unordered_map 使用哈希桶，平均查询 O(1)。", "deque 分段连续，支持两端快速插入，但整体不是单一连续缓冲区。"],
    boundary: "容器修改可能使迭代器、引用或指针失效，每种容器和操作规则不同，必须按接口文档判断。",
    verify: "针对真实访问模式比较 vector、deque、list，并记录时间、分配次数和缓存未命中，而不只看大 O。"
  },
  "iterators": {
    mechanism: "迭代器把位置和移动能力抽象出来，使算法不依赖具体容器。标准范围采用 [first,last)，长度为零时 first==last。",
    details: ["输入迭代器只保证单遍读取；随机访问迭代器才支持 it+n 和常数时间距离。", "end 是尾后哨兵，只用于比较，永远不能解引用。", "const_iterator 限制通过迭代器修改元素，但不一定让底层容器完全不可变。"],
    boundary: "失效迭代器即使地址看起来未变也不能继续使用；debug iterator 模式能发现部分误用。",
    verify: "对每个容器执行插入、删除、扩容并记录哪些迭代器仍有效，再与标准保证对照。"
  },
  "algorithms-lib": {
    mechanism: "标准算法接收范围和可调用对象，把遍历控制与业务条件分开。算法名称表达意图，也允许库针对迭代器类别优化实现。",
    details: ["sort 需要随机访问迭代器；list 应使用自身 sort 成员。", "remove 只把保留元素前移并返回新逻辑末尾，真正缩小容器还需 erase。", "二分算法要求范围按同一比较规则分区或排序，否则结果没有意义。"],
    boundary: "比较器必须满足严格弱序；写成 <= 会破坏算法前提并可能产生不可预测结果。",
    verify: "为比较器测试 irreflexive、asymmetric、transitive，并在乱序输入上验证算法前置条件。"
  },
  "lambdas": {
    mechanism: "每个 Lambda 都生成匿名闭包类型，捕获变量成为其数据成员，参数和函数体形成 operator()。按值捕获发生在创建闭包时，不是调用时。",
    details: ["[x] 保存副本，[&x] 保存引用关系；this 捕获涉及对象生命周期。", "mutable 允许修改闭包内部按值捕获的副本，不会修改原变量。", "无捕获 Lambda 可转换为普通函数指针；有捕获 Lambda 通常不能。"],
    boundary: "把引用捕获的 Lambda 放进异步任务或长期回调时，原对象可能早已销毁。",
    verify: "在创建和调用之间修改原变量，分别比较按值与按引用捕获的结果，并用析构日志验证生命周期。"
  },
  "string-processing": {
    mechanism: "string 拥有一段连续字符存储，string_view 仅保存指针和长度。搜索与切片通常按代码单元工作，不理解人类语言中的字符或字素。",
    details: ["substr 返回新 string 并复制；string_view::substr 只创建新视图。", "c_str 提供以零结尾的指针，但修改 string 后旧指针可能失效。", "UTF-8 变长编码使 size、下标和截断都按字节而非用户可见字符。"],
    boundary: "string_view 不延长底层对象生命周期，指向临时 string 或扩容后的旧缓冲区会悬空。",
    verify: "对英文、中文、emoji 输出字节数与 Unicode 码点数，观察直接按下标截断 UTF-8 的问题。"
  },
  "generic-programming": {
    mechanism: "泛型编程把算法写成对能力的要求，由不同类型提供这些能力。静态分派让抽象通常在优化后不产生额外运行时成本。",
    details: ["模板参数可以是类型、值或模板；不同参数组合形成不同实例。", "concept 描述可检查的语法与部分语义要求，但无法自动证明复杂业务约束。", "ADL 会根据实参类型所在命名空间参与函数查找，是 swap 等扩展点的基础。"],
    boundary: "过度泛型会放大编译时间、诊断复杂度和二进制体积；稳定边界可考虑类型擦除或虚接口。",
    verify: "为算法写最小 concept，并用多个满足/不满足的自定义类型编译验证。"
  },
  "raii": {
    mechanism: "RAII 把资源获取放进构造，把释放放进析构，利用作用域退出时的确定性析构覆盖正常返回和异常展开。",
    details: ["资源不仅是内存，还包括文件、锁、套接字、事务和临时状态。", "析构函数应 noexcept；异常展开期间再次抛出通常会 terminate。", "作用域守卫可用于没有现成所有者类型的成对操作。"],
    boundary: "RAII 管理生命周期，不自动解决共享所有权、死锁或释放顺序中的业务依赖。",
    verify: "在资源获得后每个位置抛异常，确认计数、文件句柄或锁都恢复到进入前状态。"
  },
  "smart-pointers": {
    mechanism: "unique_ptr 把独占所有权编码进类型并通过移动转移；shared_ptr 使用控制块维护强/弱引用计数，最后一个强引用销毁对象。",
    details: ["make_unique/make_shared 同时构造对象并减少异常窗口。", "shared_ptr 的引用计数操作通常原子，但所指对象本身并不因此线程安全。", "weak_ptr 不拥有对象，lock 成功后临时获得 shared_ptr，避免检查与使用之间竞态。"],
    boundary: "从同一个裸指针建立两个独立 shared_ptr 会形成两个控制块并重复释放；循环强引用会泄漏。",
    verify: "记录构造析构次数，搭建父子互相引用，再把一侧改为 weak_ptr 观察生命周期。"
  },
  "move-semantics": {
    mechanism: "移动构造从将亡对象转移资源句柄，避免深复制。std::move 只做值类别转换，是否真正移动由随后选中的函数决定。",
    details: ["被移动对象必须仍可析构和重新赋值，但具体值通常未指定。", "const 对象通常不能调用接收 T&& 的移动构造，因为移动需要修改源对象。", "容器扩容倾向使用 noexcept 移动；否则为异常保证可能退回复制。"],
    boundary: "移动小型标量与复制成本相同；盲目 std::move 返回局部变量还可能阻碍复制消除。",
    verify: "给类型的复制和移动构造计数，比较 push_back、emplace_back、reserve 与 noexcept 的影响。"
  },
  "type-deduction": {
    mechanism: "auto 大体遵循模板按值参数推导，会去掉顶层 const 和引用；auto&、const auto&、auto&& 则保留不同性质。decltype 按表达式形式返回更精确类型。",
    details: ["auto x = constValue 会得到非 const 副本，auto& x = constValue 得到 const 引用。", "decltype(name) 返回声明类型，decltype((name)) 把括号表达式视为左值并通常得到 T&。", "范围 for 使用 auto 会复制元素，const auto& 避免复制。"],
    boundary: "vector<bool> 等代理类型可能让 auto 推导出代理而不是 bool，生命周期和行为与直觉不同。",
    verify: "用 std::is_same_v 和 static_assert 验证多组 auto/decltype 推导，不依赖 IDE 悬浮提示。"
  },
  "concurrency-basics": {
    mechanism: "C++ 内存模型规定线程间哪些读写可见。没有同步的冲突访问形成数据竞争并导致未定义行为；互斥量解锁与随后加锁建立 happens-before。",
    details: ["mutex 保护一组不变量，lock_guard/scoped_lock 用 RAII 释放锁。", "atomic 保证单个原子对象操作不可撕裂，并提供内存序，但不能自动维护多个变量的一致关系。", "condition_variable 等待必须放在谓词循环中，因为存在虚假唤醒。"],
    boundary: "volatile 不提供线程同步；即便结果偶尔正确，数据竞争仍是错误。多锁代码还需固定顺序避免死锁。",
    verify: "用 ThreadSanitizer 运行故意无锁递增，再分别用 mutex 和 atomic 修复并解释同步关系。"
  },
  "cpp20": {
    mechanism: "C++20 用 concepts 约束模板，用 ranges 把范围作为一等接口，用 coroutine 语法描述可挂起计算；这些特性仍由库和编译器共同实现。",
    details: ["ranges view 通常惰性计算且不拥有底层数据。", "span 是连续对象的非拥有视图，包含指针和长度，不能延长数组生命周期。", "jthread 在析构时请求停止并 join，比裸 thread 更适合结构化生命周期。"],
    boundary: "语言标准开关、编译器版本和标准库版本缺一不可；可编译不代表所有库特性都完整可用。",
    verify: "用特性测试宏检查 concepts、ranges、span、jthread，并在 CI 的不同编译器上构建。"
  },
  "complexity": {
    mechanism: "渐进复杂度描述 n 增长时操作次数的数量级。它忽略常数与低阶项，但前提、输入分布和内存层次仍决定真实表现。",
    details: ["O(1) 表示与 n 无关，不表示只执行一步。", "vector 尾插为摊还 O(1)，单次扩容仍可能 O(n)。", "空间复杂度应区分输入占用、额外辅助空间和递归调用栈。"],
    boundary: "小数据上 O(n²) 可能快于高常数的 O(n log n)；先估算规模，再基准测量。",
    verify: "对 n 成倍增长记录时间并计算比值，观察线性、n log n、平方增长的不同趋势。"
  },
  "linear-structures": {
    mechanism: "线性结构规定元素的相邻关系和允许操作。数组利用连续存储换取随机访问，链表利用节点链接换取已知位置插删。",
    details: ["栈限制为一端操作，天然匹配递归、撤销和语法匹配。", "队列分离生产与消费顺序，deque 常适合作为底层。", "链表节点额外保存指针且局部性差，理论 O(1) 插入前还常需 O(n) 找位置。"],
    boundary: "选择结构应从访问模式和硬件成本出发，不能只背复杂度表。",
    verify: "实现同一工作负载的 vector 与 list 版本，记录遍历、随机访问和中间插入的真实性能。"
  },
  "trees-heaps": {
    mechanism: "树通过父子边表达层次。二叉堆是一棵存于数组中的完全二叉树，只维护父节点与孩子的偏序，不维护整体有序。",
    details: ["数组下标 i 的孩子通常是 2i+1、2i+2，父亲是 (i-1)/2。", "priority_queue 默认最大堆，top O(1)，push/pop O(log n)。", "平衡搜索树保持高度 O(log n)，普通二叉搜索树在有序输入下可退化为链。"],
    boundary: "递归遍历的额外空间是树高；极深或不受信输入应考虑显式栈。",
    verify: "画出每次堆插入/弹出后的数组与树形，并检查堆序性质，而不是期待数组完全排序。"
  },
  "hashing": {
    mechanism: "哈希函数把键映射到哈希值，容器再根据桶数定位桶并处理碰撞。相等键必须得到相同哈希值。",
    details: ["负载因子等于元素数/桶数，过高会增加碰撞并触发 rehash。", "rehash 会重建桶并使迭代器失效，但元素引用在标准保证下可能仍有效。", "operator[] 在键缺失时插入默认值，只查询可用 find、contains 或 at。"],
    boundary: "平均 O(1) 依赖合理哈希和输入；攻击者可构造碰撞使性能退化，安全场景需防护。",
    verify: "打印 bucket_count 与 load_factor，逐步插入并观察 rehash 节点及迭代器变化。"
  },
  "graphs": {
    mechanism: "图把对象建模为顶点、关系建模为边。邻接表只存实际边，空间 O(V+E)；邻接矩阵用 O(V²) 换取常数时间边查询。",
    details: ["BFS 使用队列按距离层扩展，可求无权图最短路。", "DFS 使用递归或显式栈，适合连通分量、环检测和拓扑关系。", "访问标记通常在入队/入栈时设置，避免重复加入。"],
    boundary: "有权最短路需根据负权边选择 Dijkstra、Bellman-Ford 等算法；不能直接套 BFS。",
    verify: "在非连通、有环、重复边和孤立点图上运行遍历，记录 parent 并还原实际路径。"
  },
  "search-dp": {
    mechanism: "二分利用单调谓词每次排除一半范围；动态规划把问题表示为状态，并按依赖顺序复用子问题结果。",
    details: ["lower_bound 返回第一个不小于目标的位置，范围必须按同一比较规则排序。", "二分使用左闭右开区间可让空区间和结束条件更统一。", "DP 需要明确状态含义、转移、初值、计算顺序和答案位置。"],
    boundary: "能写转移方程不等于状态正确；必须证明状态包含做出后续决策所需的全部信息。",
    verify: "先用小规模暴力递归生成真值，再与记忆化和迭代 DP 的每个状态对照。"
  },
  "git-cmake": {
    mechanism: "Git 用提交对象记录内容快照与父关系；CMake 生成具体构建系统，并通过 target 传播包含目录、编译选项和链接依赖。",
    details: ["commit 是不可变快照，branch 只是指向提交的可移动引用。", "PUBLIC、PRIVATE、INTERFACE 描述依赖是否用于目标自身及是否传给使用者。", "out-of-source 构建把生成物与源码分开，便于多配置和清理。"],
    boundary: "CMake 配置阶段和实际编译阶段不同；修改 CMakeLists 后需重新配置，缓存也可能保留旧探测结果。",
    verify: "从空目录 clone 后只按 README 命令构建测试，确保没有依赖本机 IDE 隐式设置。"
  },
  "testing": {
    mechanism: "测试通过固定输入和可观察结果建立行为契约。单元测试缩小故障范围，集成测试验证边界协作，端到端测试验证用户路径。",
    details: ["Arrange-Act-Assert 让准备、行为、验证清晰分离。", "边界值、等价类和失败路径通常比大量随机正常值更有信息。", "依赖时间、随机数、文件和网络的代码应注入可控边界。"],
    boundary: "覆盖率只说明哪些代码执行过，不证明断言正确或需求覆盖；过度模拟也会测试实现而非行为。",
    verify: "故意破坏实现，确认测试确实失败；若不失败，说明断言或场景没有保护目标行为。"
  },
  "linux": {
    mechanism: "进程通过系统调用请求内核管理文件、内存、进程和网络。文件描述符是进程内的小整数句柄，0/1/2 通常对应标准输入、输出、错误。",
    details: ["虚拟地址空间让每个进程看到独立地址布局，页表映射到物理内存或文件。", "fork 复制进程视图并常用写时复制，exec 用新程序替换当前进程映像。", "权限由用户、组和其他三组读写执行位及更高级 ACL 共同决定。"],
    boundary: "路径、大小写、动态库搜索和信号语义与 Windows 不同；跨平台代码应把系统边界集中封装。",
    verify: "用 /proc、strace 或等价工具观察程序打开文件、分配内存和创建进程时的系统调用。"
  },
  "networking": {
    mechanism: "TCP 提供有序可靠字节流，不保存应用消息边界。send 可能只发送部分数据，recv 也可能返回半条或多条消息。",
    details: ["协议必须用固定长度、分隔符或长度前缀定义帧。", "多字节整数跨网络通常使用网络字节序，并对长度设上限。", "非阻塞 I/O 的可读写事件只表示操作可能推进，不保证一次完成。"],
    boundary: "超时、半关闭、重连、背压和恶意输入都是正常状态，不应只实现连通实验中的 happy path。",
    verify: "让客户端把一条消息拆成多个小块发送，再把多条合并发送，确认服务器仍能正确组帧。"
  },
  "performance": {
    mechanism: "性能优化先用采样或追踪找到总耗时中的主要贡献，再改变一个因素并复测。CPU、缓存、分配、锁和 I/O 可能形成完全不同瓶颈。",
    details: ["平均值会隐藏长尾，服务通常还需 P50/P95/P99。", "微基准要防止编译器消除结果，并包含预热和多轮统计。", "连续数据与批处理常通过缓存局部性和减少调用开销获得提升。"],
    boundary: "基准环境、输入分布、编译选项和机器频率都会影响数字；没有环境说明的单个耗时不可复现。",
    verify: "保存基线、火焰图和修改后数据，同时运行正确性测试，避免以错误结果换速度。"
  },
  "code-quality": {
    mechanism: "代码质量来自可见契约、明确所有权、受控失败和自动反馈。编译警告、静态分析、Sanitizer 与测试分别发现不同类别问题。",
    details: ["错误处理应区分可恢复输入错误、环境失败和程序不变量破坏。", "optional 表示可能没有值，异常适合无法在本层处理的失败，结果类型适合显式传播可预期错误。", "输入边界应限制长度、范围、递归深度和资源消耗。"],
    boundary: "风格一致不能替代正确设计；安全也不是最后加一层校验，而应贯穿解析、存储和输出边界。",
    verify: "开启最高实用警告、ASan/UBSan、静态分析并把结果纳入持续集成，确保新增问题阻止合并。"
  },
  "backend-track": {
    mechanism: "后端服务把网络请求映射为受资源约束的任务，经过解析、业务、存储和响应链路；并发模型决定吞吐、延迟与故障隔离。",
    details: ["每次请求应有截止时间、取消信号、追踪标识和资源上限。", "缓存解决读延迟但引入一致性、淘汰和击穿问题。", "压测需报告并发、请求分布、机器配置、错误率与延迟分位数。"],
    boundary: "重试会放大故障，必须有幂等语义、退避和总截止时间；线程越多也不等于吞吐越高。",
    verify: "对项目注入慢数据库、断连、超时和突发流量，观察是否能限流、降级并优雅退出。"
  },
  "game-track": {
    mechanism: "游戏主循环不断处理输入、更新世界、模拟物理、提交渲染。实时系统关注每帧预算和长尾卡顿，而不仅是总吞吐。",
    details: ["固定时间步让物理模拟更稳定，渲染可在状态之间插值。", "ECS 将数据组合与系统处理分离，但是否采用取决于对象规模和访问模式。", "渲染性能需理解 draw call、状态切换、GPU/CPU 同步和资源生命周期。"],
    boundary: "平均 FPS 不能代表体验；16.7ms 帧预算中偶发 80ms 长帧仍会明显卡顿。",
    verify: "记录帧时间直方图和主线程火焰图，区分 CPU、GPU、I/O 与资源加载导致的长帧。"
  },
  "embedded-track": {
    mechanism: "嵌入式程序直接面对寄存器、中断和受限存储。可预测性、功耗与最坏执行时间往往比平均性能更重要。",
    details: ["volatile 适合表示可能被硬件或中断改变的访问，但不提供原子性和跨核同步。", "中断处理应短小，耗时工作移交主循环或任务。", "交叉编译器为目标架构生成代码，调试器通过 JTAG/SWD 等接口观察设备。"],
    boundary: "动态分配、异常、RTTI 是否可用取决于项目约束；不能把桌面默认配置直接带到 MCU。",
    verify: "检查链接映射文件中的 Flash/RAM 使用量，测量栈高水位和中断最坏耗时。"
  },
  "systems-track": {
    mechanism: "系统软件位于应用与硬件/内核边界，必须同时理解 ABI、内存、并发、I/O 和故障恢复。抽象仍要能映射到实际成本。",
    details: ["ABI 规定调用约定、名称修饰和对象布局，跨编译器边界需谨慎。", "mmap、页缓存和异步 I/O 会改变数据路径与性能特征。", "无锁结构除原子性外还涉及内存回收、ABA 和进度保证。"],
    boundary: "优化依赖具体硬件和操作系统；任何“零拷贝”“无锁”结论都应说明边界与测量证据。",
    verify: "从一个系统调用开始画出用户态、内核、缓存和设备之间的数据路径，再用追踪工具核对。"
  },
  "portfolio": {
    mechanism: "作品集的目标是让他人低成本验证你的问题拆解、技术取舍和交付能力。可运行代码只是证据链的一部分。",
    details: ["README 应先说明问题、目标和最小运行步骤，再展示架构与结果。", "性能数字必须包含环境、数据集、基线和测量方法。", "提交历史、测试和 issue 能展示迭代过程，而不是一次性上传成品。"],
    boundary: "项目越大不一定越好；一个范围受控、边界完整、能深入解释的项目比半成品平台更可信。",
    verify: "让同学只按 README 在干净环境运行项目，并记录所有需要口头补充的步骤。"
  },
  "interview": {
    mechanism: "技术面试同时评估知识、推理和沟通。好的回答先澄清假设，再给方案、复杂度、边界和验证，而不是立即默写代码。",
    details: ["项目题要能讲清背景、个人决策、困难、证据和复盘。", "算法题先定义输入输出和边界，再写可测试的小步骤。", "不知道时明确已知与未知，并提出验证路径，比猜测标准条文更可靠。"],
    boundary: "简历数字必须真实可解释；本地基准应明确标注，不伪装成生产结果。",
    verify: "录制一次 45 分钟模拟面试，复盘术语含糊、假设遗漏和未验证的结论，形成错题表。"
  }
};
